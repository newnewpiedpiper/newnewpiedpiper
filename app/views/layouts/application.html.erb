<!DOCTYPE html>
<html>
<head>
  <title>sling</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<header>
  <button id=sidebar class="menu_toggle" onclick="w3_open()", id = "Side Bar">☰</button>

  <div class="site_title"><%= link_to "Sling", root_path, id: "Sling" %></div>
  <div class="menu_right_align">
  <%= form_tag(search_page_path, :method => "get",   
    class: 'search_form') do %>  
    <div class="input-group">  
      <%= search_field_tag :search, params[:search], placeholder: "Search", class: "form-control" %>  
      <div class="input-group-btn">  
        <%= button_tag "", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>  
      </div>  
    </div>  
  <% end %> 

  <% if logged_in? %> 
   
 

  <% else %>
  
    <div class="login_links">
    <%= link_to "Log In", login_path, id:"Log In" %>
    <br>
    <%= link_to "Sign Up", new_user_path, id:"Sign Up" %>
    </div>
  <% end %>
  </div>
  
</header>
<body>
<div class="w3-sidebar w3-bar-block w3-border-right" style="display:none" id="mySidebar">
  <a onclick="w3_close()" class="w3-bar-item w3-large">Close &times;</a><br>
  <%= link_to "Home", root_path %><br>
 
  
  
  
  <% if logged_in? %>
  <%= gravatar_for current_user %>
  <%= current_user.username %>
  <br><br>
  
   <%= link_to "Profile", current_user %>

   
      
  <%= link_to "Create new post", new_post_path, id: "create_post" %><br>
   
   
    
    
     
   <%= link_to "Log Out", logout_path, method: :delete, id: "Log Out" %>
    
  <% else %>
   
   
  
  
   
  <% end %>
  
</div>
    <div class="container">
      <% flash.each do |message_type, message| %>
      <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
     
      <% end %>
    </div>
    
<%= yield %>


</body>
</html>
